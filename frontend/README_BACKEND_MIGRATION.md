# Frontend integration

The React UI and CSS are intentionally unchanged. Only the API base URL needs to point to the new FastAPI server.

Local `.env` example:

```env
VITE_API_URL=http://localhost:8000/api
```
