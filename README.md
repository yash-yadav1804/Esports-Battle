# Esports Battle

Existing React frontend + new FastAPI/PostgreSQL backend.

- `frontend/` — existing UI, preserved.
- `backend/` — FastAPI + SQLAlchemy + PostgreSQL backend.

The migration keeps the existing frontend API paths where practical.
